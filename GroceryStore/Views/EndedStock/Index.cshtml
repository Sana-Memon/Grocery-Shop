@model GroceryStore.Controllers.Dto.UserDto

@{
    ViewBag.User = Model.User;
    Layout = "~/Views/Shared/AdminDashboard.cshtml";
    @*Layout = null;*@

}



<div id="page-wrapper">
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Stock About to End</h1>
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->
        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        Stock About to End
                    </div>
                    <div class="panel-body">
                        <div class="row">
                            <div class="col-lg-6">

                              
                                @if (Model.Products.Count() > 0)
                                {
                                    <table class="table">
                                        <tr>
                                            <th>
                                                @Html.DisplayNameFor(model => model.Products[0].product_name)
                                            </th>
                                            <th>
                                                @Html.DisplayNameFor(model => model.Products[0].ImageFilePath)
                                            </th>
                                            <th>
                                                @Html.DisplayNameFor(model => model.Products[0].StockAmount)
                                            </th>
                                            <th>
                                                @Html.DisplayNameFor(model => model.Products[0].MartLocation)
                                            </th>

                                            <th>
                                                @Html.DisplayNameFor(model => model.Products[0].Category.category1)
                                            </th>

                                            <th></th>
                                        </tr>

                                        @foreach (var item in Model.Products)
                                        {
                                            <tr>
                                                <td>
                                                    @Html.DisplayFor(modelItem => item.product_name)
                                                </td>
                                                <td>
                                                    <img src="@Html.DisplayFor(modelItem => item.ImageFilePath)" alt="" width="50px" height="50px" />
                                                </td>

                                                <td>
                                                    @Html.DisplayFor(modelItem => item.StockAmount)
                                                </td>
                                                <td>
                                                    @Html.DisplayFor(modelItem => item.MartLocation)
                                                </td>

                                                <td>
                                                    @Html.DisplayFor(modelItem => item.Category.category1)
                                                </td>

                                            </tr>
                                        }

                                    </table>

                                }
                            </div>
                            <!-- /.container-fluid -->
                        </div>
                        <!-- /#page-wrapper -->

                    </div>
